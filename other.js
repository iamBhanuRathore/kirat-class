function getChartData() {}

async function setTrades(trades, chartId) {
  // Query To put the lines in the
}

let a = SqlConnection(
  "server=PES9\\SQLEXPRESS;database=StockTrade;uid=sa;password=Perfect@SSRM004;MultipleActiveResultSets=True"
);

// process.env.URL || "http"
